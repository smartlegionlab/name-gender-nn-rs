[package]
name = "name-gender-nn-rs"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Gender classifier for first names — Rust port"
license = "BSD-3-Clause"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.5", features = ["derive"] }
regex = "1.10"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1